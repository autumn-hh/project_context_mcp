# Patch Notes

每次向 GitHub 推送代码前，都必须在此文件中补充本次推送的用户可见修复、行为变化、重要实现和验证结果。

## 2026-08-07 - project_context 上下文预算与检索限额

### 修复内容

- `project_context`、`resume-project-task`、App 和 CLI 的隐式默认预算统一为 3,000 tokens；恢复任务提示显式传入该预算。
- 上下文组装先按任务检索最多 24 个 search hits，再读取命中的 active memories，并以最多 64 条最近记忆补充候选，不再预加载最多 200 条历史后统一截断。
- `in_progress` task 候选限制为 20 条并按任务相关性选择最多 10 条；代码关系限制为最多 12 个符号、40 条关系。
- 超过约 2,000 字符的 MCP `TextContent` 不再重复嵌入完整结构化结果，完整数据保留在 `structuredContent.result`。
- 新增大规模 active memories、默认预算、resume prompt 预算和大结果文本压缩回归测试。

### 验证结果

- `npm run typecheck`：通过。
- `npm test`：8 个测试文件、56 个测试全部通过。
- `npm run build`：通过。
- `git diff --check`：通过。

### 行为说明

需要更完整项目快照的调用仍可显式传入更高的 `budgetTokens`；候选限制只缩小默认检索范围，不改变数据库中的长期记忆和任务记录。

## 2026-08-04 - 任务记忆候选降噪

### 修复内容

- 每个已完成任务最多生成一条待审核记忆，依次优先使用任务摘要、第一条风险和第一条明确耐久的已完成事项，不再把单个 checkpoint 拆成最多八条候选。
- 同一任务重复调用完成接口时，不会继续生成其他回退候选；已接受、拒绝或仍待审的任务候选都会阻止重复生成。
- 重复拒绝已经处于 rejected 状态的候选会幂等返回原记录，与 MCP 工具的幂等声明保持一致；已接受、已取代或不存在的候选仍会报错。
- 新增任务候选优先级、单任务数量上限、重复完成和重复拒绝的回归测试。

### 验证结果

- `npm run typecheck`：通过。
- `npm test -- --run tests/integration.test.ts`：21 个测试通过。
- `npm test -- --run tests/ui.test.ts`：3 个测试通过。
- `npm test`：8 个测试文件、54 个测试全部通过。
- `npm run build`：通过。

### 行为说明

任务 checkpoint 仍完整保存执行进度；长期记忆审核只接收每个任务最有价值的一条候选，从源头减少任务结束时连续拒绝普通执行记录的情况。

## 2026-07-27 - 工作台初始化性能修复

### 修复内容

- 工作台和 MCP 创建应用实例时，已有项目数据库只读取 SQLite Schema 版本与稳定项目身份元数据，不再对所有已登记数据库同步执行全库 `quick_check` 和 `foreign_key_check`。
- 缺少稳定项目身份的旧数据库仍会执行一次完整校验、迁移和身份回填；首次注册、备份、恢复和数据库迁移路径继续保留完整校验。
- 项目目录移动发现改用轻量只读身份查询，避免扫描允许目录时对每个候选大型数据库执行完整性遍历。
- 显式 `project_doctor` 增加外键完整性检查，因此启动快路径不会移除数据库诊断能力。
- 新增回归测试，确认例行启动不会因完整性扫描阻塞，并确认显式诊断仍可发现外键违规。

### 验证结果

- `npm run typecheck`：通过。
- `npm test -- --run tests/personal-storage.test.ts`：13 个测试通过。
- `npm test -- --run tests/ui.test.ts`：3 个测试通过。
- `npm test`：53 个测试全部通过。
- `npm run build`：通过。
- 使用包含约 3.42 GB、840 MB 和 825 MB 项目数据库的真实注册表验证：`/api/bootstrap` 连续三次返回 HTTP 200、11,844 bytes，耗时分别为 126.9 ms、100.3 ms 和 101.1 ms；修复前基线约为 54.5 秒。

### 行为说明

例行启动不再隐式执行数据库页级和外键完整性扫描；需要完整诊断时使用 `project_doctor`。缺少 `project_id` 的旧数据库在首次打开时仍可能执行一次完整校验。

## 2026-07-24 - 项目根目录自动恢复

### 修复内容

- 项目数据库新增稳定项目身份元数据；新建、打开、迁移和恢复项目时会写入或校验该身份，旧项目在目录仍可访问时自动回填。
- Web 刷新项目数据时，如果注册的根目录已经不存在，会在配置的允许项目目录内查找对应的 `.project-context/project.db`，无需再次手动输入新路径。
- 自动恢复只接受项目身份精确匹配的唯一候选；找不到、身份错误、目录冲突或存在多个副本时不会猜测，继续保留“编辑项目”手动处理入口。
- 自动发现只遍历允许目录，跳过版本控制目录、依赖和构建输出，并对候选数据库执行完整性、结构版本和项目身份校验。
- 默认项目名称会随根目录名称一起更新；用户自定义的显示名称保持不变。
- 自动恢复后重新绑定活跃文件 watcher，并保留原有防抖配置。

### 验证结果

- `npm run typecheck`：通过。
- `npm test -- --run tests/personal-storage.test.ts tests/ui.test.ts`：14 个测试通过。
- `npm test -- --run tests/personal-storage.test.ts`：新增回填测试后 12 个测试通过。
- `npm test`：52 个测试全部通过。
- `npm run build`：通过。

### 使用说明

在允许项目目录范围内手动重命名或移动项目根目录后，刷新 Web 页面即可自动同步新路径。若移动到了未授权目录、复制出了多个带相同身份的项目数据库，或数据库尚未写入稳定身份，系统会保持原记录不变，此时可使用“编辑项目”明确指定目录。

## 2026-07-24 - 项目目录重命名同步修复

### 修复内容

- 在 Web 项目画像中新增“编辑项目”入口，可同时更新项目显示名称和根目录。
- 保存项目资料后，同步刷新任务流水线、项目画像、规则和上下文中的项目选择数据。
- 项目根目录迁移后，将活跃文件 watcher 重新绑定到新目录，并保留原有防抖配置。
- 新根目录继续使用现有真实路径、允许目录和项目冲突校验，避免越权或错误指向。
- 移动端项目操作区调整为两列布局，保证新增操作不会产生溢出。

### 验证结果

- `npm run typecheck`：通过。
- `npm test -- --run tests/ui.test.ts tests/personal-storage.test.ts`：11 个测试通过。
- `npm test`：48 个测试全部通过。
- `npm run build`：通过。
- Playwright 桌面端和 `390x844` 移动端验证通过，浏览器控制台无错误或警告。

### 使用说明

外部重命名项目目录后，在 Web 的“项目画像”中选择项目，点击“编辑项目”，填写新的项目名称和已存在的新根目录并保存。系统无法仅根据失效的旧路径通用推断任意新路径，因此外部改名后需要明确提交一次新目录。
